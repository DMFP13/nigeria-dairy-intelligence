# AGENTS

## Agent Instructions

- Keep changes modular and scoped to the requested task.
- Place product UI and API code under `app/` and `dashboard/`.
- Keep analytics modules in `analytics/` and connectors in `integrations/`.
- Store only generated artifacts in `outputs/`.
- Prefer clear, documented, testable Python code.
- Do not introduce production secrets or credentials into the repository.
